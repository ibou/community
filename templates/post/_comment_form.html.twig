{#
By default, forms enable client-side validation. This means that you can't
test the server-side validation errors from the browser. To temporarily
disable this validation, add the 'novalidate' attribute:

{{ form_start(form, {method: ..., action: ..., attr: {novalidate: 'novalidate'}}) }}
#}
{# {% set parent_id = comment is not null ? comment.id : null %} #}
{{ form_start(form, {method: 'POST', action: path('comment_new', {'postSlug': post.slug}),name: parent_id}) }}

<div class="form-row">
    <div class="col-1">
        <img class="ui_identity_user_photo" src="https://qph.fs.quoracdn.net/main-thumb-139737618-50-xgvifbpmbjofgrqkrrasrloqyvucmwqy.jpeg"
    alt="Ibou Diallo" height="45px" width="35px">
    </div>
    <div class="col">
        {{ form_widget(form.content, {attr: {rows: 1,'placeholder': 'label.addcomment'|trans }}) }}
        {{ form_help(form.content) }}
    </div>
    <div class="col">
<input id="parent_{{parent_id}}" type="hidden" value="{{parent_id}}" name="parent">
        <button class="btn btn-primary publish-comment" type="submit">
            <i class="fa fa-paper-plane" aria-hidden="true"></i> {{ 'action.publish_comment'|trans }}
        </button>
    </div>
</div>
{{ form_end(form) }}